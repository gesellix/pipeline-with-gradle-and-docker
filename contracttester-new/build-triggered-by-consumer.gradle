logger.lifecycle "configure contract tests triggered by Consumer"

runProducerContainer.doFirst {
  def producerVersion = producerVersionResolver.resolveProductionVersion()
  logger.lifecycle "producerVersion: ${producerVersion}"
  runProducerContainer.tag = producerVersion
}
